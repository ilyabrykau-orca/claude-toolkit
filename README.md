# orca-env plugin

Auto-activates Serena workspace by cwd and injects tool-routing rules at every session start.

## Install

Add as local marketplace, then `/plugin install orca-env@local`.

## Test

```bash
bash tests/run-all.sh
```
